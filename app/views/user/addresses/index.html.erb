<p style="color: green"><%= notice %></p>

<% content_for :title, "Addresses" %>

<h1>My Addresses</h1>

<div id="addresses">
  <% @addresses.each do |address| %>
    <%= render address %>
    <p>
      <%= link_to "Show this address", user_address_path(address) %>
    </p>
  <% end %>
</div>

<%= link_to "New address", new_user_address_path %>
